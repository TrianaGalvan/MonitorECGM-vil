<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="colorControlNormal">@color/inputs</item>
    </style>

    <style name="LoginTheme" parent="AppTheme">
        <item name="colorControlActivated">@color/colorAccent</item>
    </style>

    <style name="ThemeMainActivity" parent="AppTheme">
        <item name="android:textColorSecondary">#fff</item>
        <item name="actionMenuTextColor">@android:color/white</item>
    </style>

    <style name="ThemeRegistrarse" parent="AppTheme">
        <item name="colorControlNormal">@color/inputs</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="android:textSize">17dp</item>
        <item name="android:textColorHint">@color/secondary_text</item>
    </style>

    <style name="ThemeDetallePrueba" parent="AppTheme">
        <item name="colorControlNormal">@color/inputs</item>
        <item name="colorControlActivated">@color/colorAccent</item>
        <item name="android:textSize">17dp</item>
        <item name="android:textColorHint">@color/secondary_text</item>
    </style>

    <style name="RecuperarContraseña" parent="AppTheme">
        <item name="android:textColor">@color/primary_text</item>
    </style>

    <style name="toolbar_theme" parent="@style/ThemeOverlay.AppCompat.Dark">
        <item name="colorControlNormal">@color/iconsText</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
